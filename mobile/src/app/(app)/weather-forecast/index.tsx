import { FC } from 'react';

import { WeatherForecastDisplay } from '@/components';

const WeatherForecastScreen: FC = () => {
  return <WeatherForecastDisplay />;
};

export default WeatherForecastScreen;
